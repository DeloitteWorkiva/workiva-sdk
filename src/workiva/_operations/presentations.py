"""Platform API — presentations namespace.

Auto-generated by codegen. Do not edit manually.
"""

from __future__ import annotations

from typing import Any, Literal, Optional

import httpx

from workiva._constants import _API
from workiva._operations._base import BaseNamespace
from workiva._pagination import (
    extract_next_link,
    paginate_all,
    paginate_all_async,
)
from workiva.models.platform import (
    MilestoneListResult,
    Presentation,
    PresentationToPdfOptions,
    PresentationToPptxOptions,
    Slide,
    SlideLayout,
    SlideLayoutsListResult,
    SlidesListResult,
)

__all__ = ["Presentations"]


class Presentations(BaseNamespace):
    """Presentations operations."""

    _api: _API = _API.PLATFORM

    def partially_update_presentation_by_id(
        self,
        *,
        presentation_id: str,
        body: list[Any],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially updates a single presentation

        Updates the properties of a
        [presentation](ref:presentations#presentation).

        This is a long running operation. Responses include a `Location` header,
        which indicates where to poll for results. For more details on long-
        running
        job polling, see [Operations endpoint](ref:getoperationbyid).


        ### Options

        | Path                              | PATCH Operations Supported
        |
        |-----------------------------------|-----------------------------------
        -|
        | `/customFields/<custom field id>` | `add`, `remove`, `replace`, `test`
        |
        | `/customFieldGroups`              | `add`, `remove`, `replace`, `test`
        |
        | `/slideCustomFieldGroups`         | `add`, `remove`, `replace`, `test`
        |
        | `/lock`                           | `replace`
        |


        ### Examples

        #### Add a custom field value

        ```json
        [
          {
            "op": "add",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva"
          }
        ]
        ```

        #### Remove a custom field value

        ```json
        [
          {
            "op": "remove",
            "path": "/customFields/com.workiva.gsr.legal_entity"
          }
        ]
        ```

        #### Replace a custom field value

        ```json
        [
          {
            "op": "replace",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva, Inc."
          }
        ]
        ```

        #### Verifying customFieldGroup is empty before replacing the list

        ```json
        [
          {
            "op": "test",
            "path": "/customFieldGroups",
            "value": []
          },
          {
            "op": "replace",
            "path": "/customFieldGroups",
            "value": ["gsr.reporting"]
          }
        ]
        ```

        #### Adding a customFieldGroup to the end of a list

        ```json
        [
          {
            "op": "add",
            "path": "/customFieldGroups/-",
            "value": "gsr.reporting"
          }
        ]
        ```

        Args:
            presentation_id: The unique identifier of the presentation
            body: Request body.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``client.wait(response).result()`` to poll until completion.
        """
        return self._client.request(
            "PATCH",
            self._api,
            "/presentations/{presentationId}",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def partially_update_presentation_by_id_async(
        self,
        *,
        presentation_id: str,
        body: list[Any],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially updates a single presentation (async)

        Updates the properties of a
        [presentation](ref:presentations#presentation).

        This is a long running operation. Responses include a `Location` header,
        which indicates where to poll for results. For more details on long-
        running
        job polling, see [Operations endpoint](ref:getoperationbyid).


        ### Options

        | Path                              | PATCH Operations Supported
        |
        |-----------------------------------|-----------------------------------
        -|
        | `/customFields/<custom field id>` | `add`, `remove`, `replace`, `test`
        |
        | `/customFieldGroups`              | `add`, `remove`, `replace`, `test`
        |
        | `/slideCustomFieldGroups`         | `add`, `remove`, `replace`, `test`
        |
        | `/lock`                           | `replace`
        |


        ### Examples

        #### Add a custom field value

        ```json
        [
          {
            "op": "add",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva"
          }
        ]
        ```

        #### Remove a custom field value

        ```json
        [
          {
            "op": "remove",
            "path": "/customFields/com.workiva.gsr.legal_entity"
          }
        ]
        ```

        #### Replace a custom field value

        ```json
        [
          {
            "op": "replace",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva, Inc."
          }
        ]
        ```

        #### Verifying customFieldGroup is empty before replacing the list

        ```json
        [
          {
            "op": "test",
            "path": "/customFieldGroups",
            "value": []
          },
          {
            "op": "replace",
            "path": "/customFieldGroups",
            "value": ["gsr.reporting"]
          }
        ]
        ```

        #### Adding a customFieldGroup to the end of a list

        ```json
        [
          {
            "op": "add",
            "path": "/customFieldGroups/-",
            "value": "gsr.reporting"
          }
        ]
        ```

        Args:
            presentation_id: The unique identifier of the presentation
            body: Request body.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``await client.wait(response).result_async()`` to poll until completion.
        """
        return await self._client.request_async(
            "PATCH",
            self._api,
            "/presentations/{presentationId}",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_presentation_by_id(
        self,
        *,
        presentation_id: str,
        revision: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> Presentation:
        """Retrieve a single presentation

        Retrieves a presentation given its ID.

        Args:
            presentation_id: The unique identifier of the presentation
            revision: Returns resources at a specific revision
            timeout: Override the default request timeout (seconds).

        Returns:
            Presentation

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """
        response = self._client.request(
            "GET",
            self._api,
            "/presentations/{presentationId}",
            path_params={
                "presentationId": presentation_id,
            },
            query_params={
                "$revision": revision,
            },
            timeout=timeout,
        )
        return Presentation.model_validate(response.json())

    async def get_presentation_by_id_async(
        self,
        *,
        presentation_id: str,
        revision: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> Presentation:
        """Retrieve a single presentation (async)

        Retrieves a presentation given its ID.

        Args:
            presentation_id: The unique identifier of the presentation
            revision: Returns resources at a specific revision
            timeout: Override the default request timeout (seconds).

        Returns:
            Presentation

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """
        response = await self._client.request_async(
            "GET",
            self._api,
            "/presentations/{presentationId}",
            path_params={
                "presentationId": presentation_id,
            },
            query_params={
                "$revision": revision,
            },
            timeout=timeout,
        )
        return Presentation.model_validate(response.json())

    def presentation_export(
        self,
        *,
        presentation_id: str,
        format_: Literal["pdf", "pptx"],
        pdf_options: Optional[PresentationToPdfOptions] = None,
        pptx_options: Optional[PresentationToPptxOptions] = None,
        slides: Optional[list[str]] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a presentation export

        Asynchronously export a presentation as .PDF or .PPTX. Options are
        specified using a
        [PresentationExport](ref:presentations#presentationexport) object.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see [
        Operations endpoint ](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).

        Args:
            presentation_id: The unique identifier of the presentation
            format_: The file format to export the presentation as - .PDF or .PPTX.
            pdf_options:
            pptx_options:
            slides: The IDs of the slides of the presentation to export. Omit to export the entire presentation.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``client.wait(response).result()`` to poll until completion.
        """
        _body: dict[str, Any] = {}
        if format_ is not None:
            _body["format"] = format_
        if pdf_options is not None:
            _body["pdfOptions"] = pdf_options
        if pptx_options is not None:
            _body["pptxOptions"] = pptx_options
        if slides is not None:
            _body["slides"] = slides
        return self._client.request(
            "POST",
            self._api,
            "/presentations/{presentationId}/export",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=_body or None,
            timeout=timeout,
        )

    async def presentation_export_async(
        self,
        *,
        presentation_id: str,
        format_: Literal["pdf", "pptx"],
        pdf_options: Optional[PresentationToPdfOptions] = None,
        pptx_options: Optional[PresentationToPptxOptions] = None,
        slides: Optional[list[str]] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a presentation export (async)

        Asynchronously export a presentation as .PDF or .PPTX. Options are
        specified using a
        [PresentationExport](ref:presentations#presentationexport) object.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see [
        Operations endpoint ](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).

        Args:
            presentation_id: The unique identifier of the presentation
            format_: The file format to export the presentation as - .PDF or .PPTX.
            pdf_options:
            pptx_options:
            slides: The IDs of the slides of the presentation to export. Omit to export the entire presentation.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``await client.wait(response).result_async()`` to poll until completion.
        """
        _body: dict[str, Any] = {}
        if format_ is not None:
            _body["format"] = format_
        if pdf_options is not None:
            _body["pdfOptions"] = pdf_options
        if pptx_options is not None:
            _body["pptxOptions"] = pptx_options
        if slides is not None:
            _body["slides"] = slides
        return await self._client.request_async(
            "POST",
            self._api,
            "/presentations/{presentationId}/export",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=_body or None,
            timeout=timeout,
        )

    def presentation_filters_reapplication(
        self,
        *,
        presentation_id: str,
        ignore_non_editable_filters: Optional[bool] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Reapply filters to the presentation

        Performs a [`PresentationFiltersReapplication`](ref:content#presentation
        filtersreapplication) on the specified presentation.
        This endpoint is used to refresh the presentation's filters based on the
        latest state or configuration changes.
        The filters are reapplied in the context of the presentation's current
        data state.

        This is a long-running operation. Responses include a `Location` header,
        which indicates where to poll for results.
        For more details on long-running job polling, see [Operations
        endpoint](ref:getoperationbyid).

        Args:
            presentation_id: The unique identifier of the presentation
            ignore_non_editable_filters: Skip filters that cannot be reapplied (due to not having edit permissions or locked sections) instead of returning an error.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``client.wait(response).result()`` to poll until completion.
        """
        _body: dict[str, Any] = {}
        if ignore_non_editable_filters is not None:
            _body["ignoreNonEditableFilters"] = ignore_non_editable_filters
        return self._client.request(
            "POST",
            self._api,
            "/presentations/{presentationId}/filters/reapplication",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=_body or None,
            timeout=timeout,
        )

    async def presentation_filters_reapplication_async(
        self,
        *,
        presentation_id: str,
        ignore_non_editable_filters: Optional[bool] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Reapply filters to the presentation (async)

        Performs a [`PresentationFiltersReapplication`](ref:content#presentation
        filtersreapplication) on the specified presentation.
        This endpoint is used to refresh the presentation's filters based on the
        latest state or configuration changes.
        The filters are reapplied in the context of the presentation's current
        data state.

        This is a long-running operation. Responses include a `Location` header,
        which indicates where to poll for results.
        For more details on long-running job polling, see [Operations
        endpoint](ref:getoperationbyid).

        Args:
            presentation_id: The unique identifier of the presentation
            ignore_non_editable_filters: Skip filters that cannot be reapplied (due to not having edit permissions or locked sections) instead of returning an error.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``await client.wait(response).result_async()`` to poll until completion.
        """
        _body: dict[str, Any] = {}
        if ignore_non_editable_filters is not None:
            _body["ignoreNonEditableFilters"] = ignore_non_editable_filters
        return await self._client.request_async(
            "POST",
            self._api,
            "/presentations/{presentationId}/filters/reapplication",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=_body or None,
            timeout=timeout,
        )

    def presentation_links_publication(
        self,
        *,
        presentation_id: str,
        publish_type: Literal["ownLinks", "allLinks"],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate publication of links in a presentation

        Publishes the links in a presentation - either all (as document owner)
        or only one's own. Content at the latest presentation revision will be
        used for publish.
        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).

        Args:
            presentation_id: The unique identifier of the presentation
            publish_type: Whether or not all links should be published. When "ownLinks" only links one has last edited are published. When "allLinks" all links in document or presentation are published.

            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``client.wait(response).result()`` to poll until completion.
        """
        _body: dict[str, Any] = {}
        if publish_type is not None:
            _body["publishType"] = publish_type
        return self._client.request(
            "POST",
            self._api,
            "/presentations/{presentationId}/links/publication",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=_body or None,
            timeout=timeout,
        )

    async def presentation_links_publication_async(
        self,
        *,
        presentation_id: str,
        publish_type: Literal["ownLinks", "allLinks"],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate publication of links in a presentation (async)

        Publishes the links in a presentation - either all (as document owner)
        or only one's own. Content at the latest presentation revision will be
        used for publish.
        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).

        Args:
            presentation_id: The unique identifier of the presentation
            publish_type: Whether or not all links should be published. When "ownLinks" only links one has last edited are published. When "allLinks" all links in document or presentation are published.

            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``await client.wait(response).result_async()`` to poll until completion.
        """
        _body: dict[str, Any] = {}
        if publish_type is not None:
            _body["publishType"] = publish_type
        return await self._client.request_async(
            "POST",
            self._api,
            "/presentations/{presentationId}/links/publication",
            path_params={
                "presentationId": presentation_id,
            },
            json_body=_body or None,
            timeout=timeout,
        )

    def get_presentation_milestones(
        self,
        *,
        presentation_id: str,
        timeout: Optional[float] = None,
    ) -> MilestoneListResult:
        """Retrieve a list of milestones for a presentation

        Returns [MilestoneListResult](ref:milestones#milestonelistresult).

        Args:
            presentation_id: The unique identifier of the presentation
            timeout: Override the default request timeout (seconds).

        Returns:
            MilestoneListResult

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """

        def _fetch(_cursor: str | None) -> httpx.Response:
            return self._client.request(
                "GET",
                self._api,
                "/presentations/{presentationId}/milestones",
                path_params={
                    "presentationId": presentation_id,
                },
                query_params={
                    "$next": _cursor,
                },
                timeout=timeout,
            )

        _body_result = paginate_all(_fetch, extract_next_link, "data")
        return MilestoneListResult.model_validate(_body_result)

    async def get_presentation_milestones_async(
        self,
        *,
        presentation_id: str,
        timeout: Optional[float] = None,
    ) -> MilestoneListResult:
        """Retrieve a list of milestones for a presentation (async)

        Returns [MilestoneListResult](ref:milestones#milestonelistresult).

        Args:
            presentation_id: The unique identifier of the presentation
            timeout: Override the default request timeout (seconds).

        Returns:
            MilestoneListResult

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """

        async def _fetch(_cursor: str | None) -> httpx.Response:
            return await self._client.request_async(
                "GET",
                self._api,
                "/presentations/{presentationId}/milestones",
                path_params={
                    "presentationId": presentation_id,
                },
                query_params={
                    "$next": _cursor,
                },
                timeout=timeout,
            )

        _body_result = await paginate_all_async(_fetch, extract_next_link, "data")
        return MilestoneListResult.model_validate(_body_result)

    def get_slide_layouts(
        self,
        *,
        presentation_id: str,
        revision: Optional[str] = None,
        maxpagesize: Optional[int] = 1000,
        timeout: Optional[float] = None,
    ) -> SlideLayoutsListResult:
        """Retrieve a list of slide layouts

        Returns a list of slide layouts.

        Args:
            presentation_id: The unique identifier of the presentation
            revision: Returns resources at a specific revision
            maxpagesize: The maximum number of results to retrieve
            timeout: Override the default request timeout (seconds).

        Returns:
            SlideLayoutsListResult

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """

        def _fetch(_cursor: str | None) -> httpx.Response:
            return self._client.request(
                "GET",
                self._api,
                "/presentations/{presentationId}/slideLayouts",
                path_params={
                    "presentationId": presentation_id,
                },
                query_params={
                    "$revision": revision,
                    "$maxpagesize": maxpagesize,
                    "$next": _cursor,
                },
                timeout=timeout,
            )

        _body_result = paginate_all(_fetch, extract_next_link, "data")
        return SlideLayoutsListResult.model_validate(_body_result)

    async def get_slide_layouts_async(
        self,
        *,
        presentation_id: str,
        revision: Optional[str] = None,
        maxpagesize: Optional[int] = 1000,
        timeout: Optional[float] = None,
    ) -> SlideLayoutsListResult:
        """Retrieve a list of slide layouts (async)

        Returns a list of slide layouts.

        Args:
            presentation_id: The unique identifier of the presentation
            revision: Returns resources at a specific revision
            maxpagesize: The maximum number of results to retrieve
            timeout: Override the default request timeout (seconds).

        Returns:
            SlideLayoutsListResult

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """

        async def _fetch(_cursor: str | None) -> httpx.Response:
            return await self._client.request_async(
                "GET",
                self._api,
                "/presentations/{presentationId}/slideLayouts",
                path_params={
                    "presentationId": presentation_id,
                },
                query_params={
                    "$revision": revision,
                    "$maxpagesize": maxpagesize,
                    "$next": _cursor,
                },
                timeout=timeout,
            )

        _body_result = await paginate_all_async(_fetch, extract_next_link, "data")
        return SlideLayoutsListResult.model_validate(_body_result)

    def partially_update_slide_layout_by_id(
        self,
        *,
        presentation_id: str,
        slide_layout_id: str,
        body: list[Any],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially update a single slide layout

        Updates one or more properties of a single slide layout.

        This is a long running operation. Responses include a `Location` header,
        which indicates where to poll for results. For more details on long-
        running
        job polling, see [Operations endpoint](ref:getoperationbyid).

        ### Options

        | Path            | PATCH Operations Supported |
        |-----------------|----------------------------|
        | `/name`         | `replace`                  |
        | `/index`        | `replace`                  |
        | `/lock`         | `replace`                  |

        Args:
            presentation_id: The unique identifier of the presentation
            slide_layout_id: The unique identifier of the slide layout
            body: Request body.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``client.wait(response).result()`` to poll until completion.
        """
        return self._client.request(
            "PATCH",
            self._api,
            "/presentations/{presentationId}/slideLayouts/{slideLayoutId}",
            path_params={
                "presentationId": presentation_id,
                "slideLayoutId": slide_layout_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def partially_update_slide_layout_by_id_async(
        self,
        *,
        presentation_id: str,
        slide_layout_id: str,
        body: list[Any],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially update a single slide layout (async)

        Updates one or more properties of a single slide layout.

        This is a long running operation. Responses include a `Location` header,
        which indicates where to poll for results. For more details on long-
        running
        job polling, see [Operations endpoint](ref:getoperationbyid).

        ### Options

        | Path            | PATCH Operations Supported |
        |-----------------|----------------------------|
        | `/name`         | `replace`                  |
        | `/index`        | `replace`                  |
        | `/lock`         | `replace`                  |

        Args:
            presentation_id: The unique identifier of the presentation
            slide_layout_id: The unique identifier of the slide layout
            body: Request body.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``await client.wait(response).result_async()`` to poll until completion.
        """
        return await self._client.request_async(
            "PATCH",
            self._api,
            "/presentations/{presentationId}/slideLayouts/{slideLayoutId}",
            path_params={
                "presentationId": presentation_id,
                "slideLayoutId": slide_layout_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_slide_layout_by_id(
        self,
        *,
        presentation_id: str,
        slide_layout_id: str,
        revision: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> SlideLayout:
        """Retrieve a single slide layout

        Retrieves a slide layout given its ID.

        Args:
            presentation_id: The unique identifier of the presentation
            slide_layout_id: The unique identifier of the slide layout
            revision: Returns resources at a specific revision
            timeout: Override the default request timeout (seconds).

        Returns:
            SlideLayout

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """
        response = self._client.request(
            "GET",
            self._api,
            "/presentations/{presentationId}/slideLayouts/{slideLayoutId}",
            path_params={
                "presentationId": presentation_id,
                "slideLayoutId": slide_layout_id,
            },
            query_params={
                "$revision": revision,
            },
            timeout=timeout,
        )
        return SlideLayout.model_validate(response.json())

    async def get_slide_layout_by_id_async(
        self,
        *,
        presentation_id: str,
        slide_layout_id: str,
        revision: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> SlideLayout:
        """Retrieve a single slide layout (async)

        Retrieves a slide layout given its ID.

        Args:
            presentation_id: The unique identifier of the presentation
            slide_layout_id: The unique identifier of the slide layout
            revision: Returns resources at a specific revision
            timeout: Override the default request timeout (seconds).

        Returns:
            SlideLayout

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """
        response = await self._client.request_async(
            "GET",
            self._api,
            "/presentations/{presentationId}/slideLayouts/{slideLayoutId}",
            path_params={
                "presentationId": presentation_id,
                "slideLayoutId": slide_layout_id,
            },
            query_params={
                "$revision": revision,
            },
            timeout=timeout,
        )
        return SlideLayout.model_validate(response.json())

    def get_slides(
        self,
        *,
        presentation_id: str,
        revision: Optional[str] = None,
        maxpagesize: Optional[int] = 1000,
        timeout: Optional[float] = None,
    ) -> SlidesListResult:
        """Retrieve a list of slides

        Returns a list of slides.

        Args:
            presentation_id: The unique identifier of the presentation
            revision: Returns resources at a specific revision
            maxpagesize: The maximum number of results to retrieve
            timeout: Override the default request timeout (seconds).

        Returns:
            SlidesListResult

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """

        def _fetch(_cursor: str | None) -> httpx.Response:
            return self._client.request(
                "GET",
                self._api,
                "/presentations/{presentationId}/slides",
                path_params={
                    "presentationId": presentation_id,
                },
                query_params={
                    "$revision": revision,
                    "$maxpagesize": maxpagesize,
                    "$next": _cursor,
                },
                timeout=timeout,
            )

        _body_result = paginate_all(_fetch, extract_next_link, "data")
        return SlidesListResult.model_validate(_body_result)

    async def get_slides_async(
        self,
        *,
        presentation_id: str,
        revision: Optional[str] = None,
        maxpagesize: Optional[int] = 1000,
        timeout: Optional[float] = None,
    ) -> SlidesListResult:
        """Retrieve a list of slides (async)

        Returns a list of slides.

        Args:
            presentation_id: The unique identifier of the presentation
            revision: Returns resources at a specific revision
            maxpagesize: The maximum number of results to retrieve
            timeout: Override the default request timeout (seconds).

        Returns:
            SlidesListResult

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """

        async def _fetch(_cursor: str | None) -> httpx.Response:
            return await self._client.request_async(
                "GET",
                self._api,
                "/presentations/{presentationId}/slides",
                path_params={
                    "presentationId": presentation_id,
                },
                query_params={
                    "$revision": revision,
                    "$maxpagesize": maxpagesize,
                    "$next": _cursor,
                },
                timeout=timeout,
            )

        _body_result = await paginate_all_async(_fetch, extract_next_link, "data")
        return SlidesListResult.model_validate(_body_result)

    def partially_update_slide_by_id(
        self,
        *,
        presentation_id: str,
        slide_id: str,
        body: list[Any],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially update a single slide

        Updates one or more properties of a single slide.

        This is a long running operation. Responses include a `Location` header,
        which indicates where to poll for results. For more details on long-
        running
        job polling, see [Operations endpoint](ref:getoperationbyid).

        ### Options

        | Path                              | PATCH Operations Supported
        |
        |-----------------------------------|-----------------------------------
        -|
        | `/name`                           | `replace`
        |
        | `/index`                          | `replace`
        |
        | `/parent`                         | `replace`
        |
        | `/customFields/<custom field id>` | `add`, `remove`, `replace`, `test`
        |
        | `/lock`                           | `replace`
        |

        ### Examples

        #### Update the name of a slide

        ```json
        [
          {
            "op": "replace",
            "path": "/name",
            "value": "Risk Factor"
          }
        ]
        ```

        #### Update the parent of a slide (preserving its index)

        ```json
        [
          {
            "op": "replace",
            "path": "/parent",
            "value": {
              "id": "b9b3ddb587744a27aafda3c9865f1f0a_1"
            }
          }
        ]
        ```

        #### Update the parent of a slide (making it the first child)

        ```json
        [
          {
            "op": "replace",
            "path": "/parent",
            "value": {
              "id": "b9b3ddb587744a27aafda3c9865f1f0a_1"
            }
          },
          {
            "op": "replace",
            "path": "/index",
            "value": 0
          }
        ]
        ```

        #### Add a custom field value

        ```json
        [
          {
            "op": "add",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva"
          }
        ]
        ```

        #### Remove a custom field value

        ```json
        [
          {
            "op": "remove",
            "path": "/customFields/com.workiva.gsr.legal_entity"
          }
        ]
        ```

        #### Replace a custom field value

        ```json
        [
          {
            "op": "replace",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva, Inc."
          }
        ]
        ```

        Args:
            presentation_id: The unique identifier of the presentation
            slide_id: The unique identifier of the slide
            body: Request body.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``client.wait(response).result()`` to poll until completion.
        """
        return self._client.request(
            "PATCH",
            self._api,
            "/presentations/{presentationId}/slides/{slideId}",
            path_params={
                "presentationId": presentation_id,
                "slideId": slide_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def partially_update_slide_by_id_async(
        self,
        *,
        presentation_id: str,
        slide_id: str,
        body: list[Any],
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially update a single slide (async)

        Updates one or more properties of a single slide.

        This is a long running operation. Responses include a `Location` header,
        which indicates where to poll for results. For more details on long-
        running
        job polling, see [Operations endpoint](ref:getoperationbyid).

        ### Options

        | Path                              | PATCH Operations Supported
        |
        |-----------------------------------|-----------------------------------
        -|
        | `/name`                           | `replace`
        |
        | `/index`                          | `replace`
        |
        | `/parent`                         | `replace`
        |
        | `/customFields/<custom field id>` | `add`, `remove`, `replace`, `test`
        |
        | `/lock`                           | `replace`
        |

        ### Examples

        #### Update the name of a slide

        ```json
        [
          {
            "op": "replace",
            "path": "/name",
            "value": "Risk Factor"
          }
        ]
        ```

        #### Update the parent of a slide (preserving its index)

        ```json
        [
          {
            "op": "replace",
            "path": "/parent",
            "value": {
              "id": "b9b3ddb587744a27aafda3c9865f1f0a_1"
            }
          }
        ]
        ```

        #### Update the parent of a slide (making it the first child)

        ```json
        [
          {
            "op": "replace",
            "path": "/parent",
            "value": {
              "id": "b9b3ddb587744a27aafda3c9865f1f0a_1"
            }
          },
          {
            "op": "replace",
            "path": "/index",
            "value": 0
          }
        ]
        ```

        #### Add a custom field value

        ```json
        [
          {
            "op": "add",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva"
          }
        ]
        ```

        #### Remove a custom field value

        ```json
        [
          {
            "op": "remove",
            "path": "/customFields/com.workiva.gsr.legal_entity"
          }
        ]
        ```

        #### Replace a custom field value

        ```json
        [
          {
            "op": "replace",
            "path": "/customFields/com.workiva.gsr.legal_entity",
            "value": "Workiva, Inc."
          }
        ]
        ```

        Args:
            presentation_id: The unique identifier of the presentation
            slide_id: The unique identifier of the slide
            body: Request body.
            timeout: Override the default request timeout (seconds).

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).

        Note:
            This is a long-running operation (HTTP 202). Use
            ``await client.wait(response).result_async()`` to poll until completion.
        """
        return await self._client.request_async(
            "PATCH",
            self._api,
            "/presentations/{presentationId}/slides/{slideId}",
            path_params={
                "presentationId": presentation_id,
                "slideId": slide_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_slide_by_id(
        self,
        *,
        presentation_id: str,
        slide_id: str,
        revision: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> Slide:
        """Retrieve a single slide

        Retrieves a slide given its ID.

        Args:
            presentation_id: The unique identifier of the presentation
            slide_id: The unique identifier of the slide
            revision: Returns resources at a specific revision
            timeout: Override the default request timeout (seconds).

        Returns:
            Slide

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """
        response = self._client.request(
            "GET",
            self._api,
            "/presentations/{presentationId}/slides/{slideId}",
            path_params={
                "presentationId": presentation_id,
                "slideId": slide_id,
            },
            query_params={
                "$revision": revision,
            },
            timeout=timeout,
        )
        return Slide.model_validate(response.json())

    async def get_slide_by_id_async(
        self,
        *,
        presentation_id: str,
        slide_id: str,
        revision: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> Slide:
        """Retrieve a single slide (async)

        Retrieves a slide given its ID.

        Args:
            presentation_id: The unique identifier of the presentation
            slide_id: The unique identifier of the slide
            revision: Returns resources at a specific revision
            timeout: Override the default request timeout (seconds).

        Returns:
            Slide

        Raises:
            WorkivaAPIError: On API errors (400, 401, 403, 404, 409, 429, 500, 503).
        """
        response = await self._client.request_async(
            "GET",
            self._api,
            "/presentations/{presentationId}/slides/{slideId}",
            path_params={
                "presentationId": presentation_id,
                "slideId": slide_id,
            },
            query_params={
                "$revision": revision,
            },
            timeout=timeout,
        )
        return Slide.model_validate(response.json())
