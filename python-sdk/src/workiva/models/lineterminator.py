"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .linecap import LineCap
from .linecapsize import LineCapSize
from .linerelativeposition import LineRelativePosition, LineRelativePositionTypedDict
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from workiva.types import BaseModel, UNSET_SENTINEL


class LineTerminatorTypedDict(TypedDict):
    r"""Defines the shape, size, and location of a line''s head and tail
    (the starting point and stop point of a line segment or connector).

    """

    position: LineRelativePositionTypedDict
    r"""The relative position for the head and tail of a line drawing element."""
    cap: NotRequired[LineCap]
    r"""The way the terminator of a line looks."""
    size: NotRequired[LineCapSize]
    r"""The size of a cap at a line terminator."""


class LineTerminator(BaseModel):
    r"""Defines the shape, size, and location of a line''s head and tail
    (the starting point and stop point of a line segment or connector).

    """

    position: LineRelativePosition
    r"""The relative position for the head and tail of a line drawing element."""

    cap: Optional[LineCap] = LineCap.NONE
    r"""The way the terminator of a line looks."""

    size: Optional[LineCapSize] = LineCapSize.MEDIUM
    r"""The size of a cap at a line terminator."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["cap", "size"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
