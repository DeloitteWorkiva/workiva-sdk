"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict
from workiva.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL


class TaskActionAction(str, Enum):
    SUBMIT = "SUBMIT"
    APPROVE = "APPROVE"
    REJECT = "REJECT"
    CANCEL = "CANCEL"
    RESTART = "RESTART"
    SKIP = "SKIP"


class TaskActionTypedDict(TypedDict):
    r"""An action to be taken on a task approval step."""

    action: TaskActionAction
    comment: NotRequired[Nullable[str]]
    r"""An optional comment to accompany the action."""


class TaskAction(BaseModel):
    r"""An action to be taken on a task approval step."""

    action: TaskActionAction

    comment: OptionalNullable[str] = UNSET
    r"""An optional comment to accompany the action."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["comment"])
        nullable_fields = set(["comment"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
