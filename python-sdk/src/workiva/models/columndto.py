"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, Optional
from typing_extensions import NotRequired, TypedDict
from workiva import models, utils
from workiva.types import BaseModel, UNSET_SENTINEL


class Mode(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""This property is for future use, currently always set to nullable"""

    NULLABLE = "nullable"
    REQUIRED = "required"
    REPEATED = "repeated"


class ColumnDtoType(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The type of column"""

    STRING = "string"
    BYTES = "bytes"
    INTEGER = "integer"
    FLOAT = "float"
    BOOLEAN = "boolean"
    TIMESTAMP = "timestamp"
    DATE = "date"
    TIME = "time"
    DATETIME = "datetime"
    GLOBAL_PARAMETER = "global_parameter"
    NESTED_QUERY_PARAMETER = "nested_query_parameter"


class ColumnDtoTypedDict(TypedDict):
    name: str
    r"""May be at most 100 characters in length"""
    type: ColumnDtoType
    r"""The type of column"""
    alias: NotRequired[str]
    r"""Optional alias, may be at most 255 characters in length"""
    description: NotRequired[str]
    r"""May be at most 255 characters in length"""
    metadata: NotRequired[Dict[str, Any]]
    r"""Optional type metadata. Current allowed parameters are: 'formatString' allows users to define their own Java8 datetime format string.'numberFormat' allows parsing of alternative decimal formats such as 1.000,00"""
    mode: NotRequired[Mode]
    r"""This property is for future use, currently always set to nullable"""


class ColumnDto(BaseModel):
    name: str
    r"""May be at most 100 characters in length"""

    type: ColumnDtoType
    r"""The type of column"""

    alias: Optional[str] = None
    r"""Optional alias, may be at most 255 characters in length"""

    description: Optional[str] = None
    r"""May be at most 255 characters in length"""

    metadata: Optional[Dict[str, Any]] = None
    r"""Optional type metadata. Current allowed parameters are: 'formatString' allows users to define their own Java8 datetime format string.'numberFormat' allows parsing of alternative decimal formats such as 1.000,00"""

    mode: Optional[Mode] = None
    r"""This property is for future use, currently always set to nullable"""

    @field_serializer("mode")
    def serialize_mode(self, value):
        if isinstance(value, str):
            try:
                return models.Mode(value)
            except ValueError:
                return value
        return value

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return models.ColumnDtoType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["alias", "description", "metadata", "mode"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
