"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .extensiontype import ExtensionType
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from workiva.types import BaseModel, UNSET_SENTINEL


class AnchorExtensionTypedDict(TypedDict):
    r"""Information about how an anchor is used. When an anchor is first created  it will not be associated with any extensions. Once an anchor is used, its  extension type will be set."""

    id: NotRequired[str]
    r"""The unique identifier of the extension. This can be used to lookup the  associated resource. This ID will be null if the resource doesn't have an ID. For example, comment extensions don't have an id because they're  looked up by their respective anchor IDs instead.

    """
    type: NotRequired[ExtensionType]
    r"""Indicates the type of the anchor extension."""


class AnchorExtension(BaseModel):
    r"""Information about how an anchor is used. When an anchor is first created  it will not be associated with any extensions. Once an anchor is used, its  extension type will be set."""

    id: Optional[str] = None
    r"""The unique identifier of the extension. This can be used to lookup the  associated resource. This ID will be null if the resource doesn't have an ID. For example, comment extensions don't have an id because they're  looked up by their respective anchor IDs instead.

    """

    type: Optional[ExtensionType] = None
    r"""Indicates the type of the anchor extension."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
