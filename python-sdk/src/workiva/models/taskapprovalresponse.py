"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from workiva.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL


class TaskApprovalResponseAction(str, Enum):
    SKIP = "SKIP"
    APPROVE = "APPROVE"
    REJECT = "REJECT"


class TaskApprovalResponseUserTypedDict(TypedDict):
    r"""A user refernenced by their unique identifier"""

    id: NotRequired[str]
    r"""The unique identifier of the user"""


class TaskApprovalResponseUser(BaseModel):
    r"""A user refernenced by their unique identifier"""

    id: Optional[str] = None
    r"""The unique identifier of the user"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class TaskApprovalResponseCreatedTypedDict(TypedDict):
    r"""When the action was performed, and details about the user who did it"""

    date_time: NotRequired[datetime]
    r"""When the action was performed"""
    user: NotRequired[Nullable[TaskApprovalResponseUserTypedDict]]


class TaskApprovalResponseCreated(BaseModel):
    r"""When the action was performed, and details about the user who did it"""

    date_time: Annotated[Optional[datetime], pydantic.Field(alias="dateTime")] = None
    r"""When the action was performed"""

    user: OptionalNullable[TaskApprovalResponseUser] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["dateTime", "user"])
        nullable_fields = set(["user"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class TaskApprovalResponseTypedDict(TypedDict):
    r"""A response to an approval step, including the user, action taken, timestamp, and optional comment."""

    action: TaskApprovalResponseAction
    created: TaskApprovalResponseCreatedTypedDict
    comment: NotRequired[Nullable[str]]
    r"""An optional comment left by the user."""


class TaskApprovalResponse(BaseModel):
    r"""A response to an approval step, including the user, action taken, timestamp, and optional comment."""

    action: TaskApprovalResponseAction

    created: TaskApprovalResponseCreated

    comment: OptionalNullable[str] = UNSET
    r"""An optional comment left by the user."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["comment"])
        nullable_fields = set(["comment"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
