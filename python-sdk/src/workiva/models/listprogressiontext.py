"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from workiva.types import BaseModel, UNSET_SENTINEL


class ListProgressionTextTypedDict(TypedDict):
    r"""The prefix and suffix text applied to the list progression glyph."""

    locked: NotRequired[bool]
    r"""Whether the prefix and suffix are locked."""
    prefix: NotRequired[str]
    r"""Prefix to be prepended to the glyph."""
    suffix: NotRequired[str]
    r"""Suffix to be appended to the glyph."""


class ListProgressionText(BaseModel):
    r"""The prefix and suffix text applied to the list progression glyph."""

    locked: Optional[bool] = None
    r"""Whether the prefix and suffix are locked."""

    prefix: Optional[str] = None
    r"""Prefix to be prepended to the glyph."""

    suffix: Optional[str] = None
    r"""Suffix to be appended to the glyph."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["locked", "prefix", "suffix"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
