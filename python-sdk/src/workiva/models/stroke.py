"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .color import Color, ColorTypedDict
from .strokepattern import StrokePattern
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from workiva.types import BaseModel, UNSET_SENTINEL


class StrokeTypedDict(TypedDict):
    r"""Line stroke setting for things like shape edges. Use a null to indicate no stroke line."""

    color: ColorTypedDict
    r"""Represents a color."""
    pattern: NotRequired[StrokePattern]
    r"""The pattern to apply to the stroke."""
    width: NotRequired[float]
    r"""The width of the stroke in points."""


class Stroke(BaseModel):
    r"""Line stroke setting for things like shape edges. Use a null to indicate no stroke line."""

    color: Color
    r"""Represents a color."""

    pattern: Optional[StrokePattern] = StrokePattern.SOLID
    r"""The pattern to apply to the stroke."""

    width: Optional[float] = 1
    r"""The width of the stroke in points."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["pattern", "width"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
