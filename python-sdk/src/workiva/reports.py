"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from jsonpath import JSONPath
from typing import Any, Awaitable, Dict, List, Mapping, Optional, Union
from workiva import errors, models, utils
from workiva._hooks import HookContext
from workiva.types import OptionalNullable, UNSET
from workiva.utils.unmarshal_json_response import unmarshal_json_response


class Reports(BaseSDK):
    r"""Endpoints used to generate Admin reports"""

    def get_org_report_users(
        self,
        *,
        organization_id: str,
        page_after: Optional[str] = None,
        page_size: Optional[int] = 100,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        url_override: Optional[str] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.GetOrgReportUsersResponse1]:
        r"""List organization users

        Similar to an organization admin's \"People View\" export, this returns a paged list of specific details about the organization's users, including their usernames, display names, email addresses, SAML IDs, last logins, workspace memberships, licenses and roles. Results are sorted by displayName. <br /><br /> To obtain an organization's users, you need its organizationId. To view the organizationId in Wdesk, click your name in the lower left, and select <strong>Organization Admin</strong>. In the URL in your browser, the organizationId appears as a GUID after the /o/ such as 371b5fc8-fcc4-404a-9d22-d07d8f46a7e0. To view the ID of another organization you manage, select <strong>Switch Organizations</strong> from the left-hand menu, and search for and select the organization. <br /><br /> <strong>Requires the calling user to be an organization admin</strong>.


        :param organization_id: The unique identifier of the organization
        :param page_after: Paging cursor
        :param page_size: Number of items to return. Maximum of 1000.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetOrgReportUsersRequest(
            organization_id=organization_id,
            page_after=page_after,
            page_size=page_size,
        )

        req = self._build_request(
            method="GET",
            path="/organizations/{organizationId}/orgReportUsers",
            base_url=base_url,
            url_variables=url_variables,
            url_override=url_override,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getOrgReportUsers",
                oauth2_scopes=["organization:read"],
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "405",
                "415",
                "4XX",
                "500",
                "501",
                "503",
                "5XX",
            ],
            retry_config=retry_config,
        )

        def next_func() -> Optional[models.GetOrgReportUsersResponse1]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            next_url_match = JSONPath("$.links.next").parse(body)
            if len(next_url_match) == 0:
                return None

            next_url = next_url_match[0]
            if not next_url:
                return None

            if next_url[0] == "/":
                url = self._get_url(base_url, url_variables)
                next_url = url + next_url

            return self.get_org_report_users(
                organization_id=organization_id,
                page_after=page_after,
                page_size=page_size,
                retries=retries,
                url_override=next_url,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.GetOrgReportUsersResponse1(
                result=unmarshal_json_response(
                    models.GetOrgReportUsersResponse, http_res
                ),
                next=next_func,
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "405", "415"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.FailureData, http_res)
            raise errors.Failure(response_data, http_res)
        if utils.match_response(http_res, ["500", "501", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.FailureData, http_res)
            raise errors.Failure(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)

        raise errors.SDKError("Unexpected response received", http_res)

    async def get_org_report_users_async(
        self,
        *,
        organization_id: str,
        page_after: Optional[str] = None,
        page_size: Optional[int] = 100,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        url_override: Optional[str] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.GetOrgReportUsersResponse1]:
        r"""List organization users

        Similar to an organization admin's \"People View\" export, this returns a paged list of specific details about the organization's users, including their usernames, display names, email addresses, SAML IDs, last logins, workspace memberships, licenses and roles. Results are sorted by displayName. <br /><br /> To obtain an organization's users, you need its organizationId. To view the organizationId in Wdesk, click your name in the lower left, and select <strong>Organization Admin</strong>. In the URL in your browser, the organizationId appears as a GUID after the /o/ such as 371b5fc8-fcc4-404a-9d22-d07d8f46a7e0. To view the ID of another organization you manage, select <strong>Switch Organizations</strong> from the left-hand menu, and search for and select the organization. <br /><br /> <strong>Requires the calling user to be an organization admin</strong>.


        :param organization_id: The unique identifier of the organization
        :param page_after: Paging cursor
        :param page_size: Number of items to return. Maximum of 1000.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetOrgReportUsersRequest(
            organization_id=organization_id,
            page_after=page_after,
            page_size=page_size,
        )

        req = self._build_request_async(
            method="GET",
            path="/organizations/{organizationId}/orgReportUsers",
            base_url=base_url,
            url_variables=url_variables,
            url_override=url_override,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getOrgReportUsers",
                oauth2_scopes=["organization:read"],
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "405",
                "415",
                "4XX",
                "500",
                "501",
                "503",
                "5XX",
            ],
            retry_config=retry_config,
        )

        def next_func() -> Awaitable[Optional[models.GetOrgReportUsersResponse1]]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            async def empty_result():
                return None

            next_url_match = JSONPath("$.links.next").parse(body)
            if len(next_url_match) == 0:
                return empty_result()

            next_url = next_url_match[0]
            if not next_url:
                return empty_result()

            if next_url[0] == "/":
                url = self._get_url(base_url, url_variables)
                next_url = url + next_url

            return self.get_org_report_users_async(
                organization_id=organization_id,
                page_after=page_after,
                page_size=page_size,
                retries=retries,
                url_override=next_url,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.GetOrgReportUsersResponse1(
                result=unmarshal_json_response(
                    models.GetOrgReportUsersResponse, http_res
                ),
                next=next_func,
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "405", "415"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.FailureData, http_res)
            raise errors.Failure(response_data, http_res)
        if utils.match_response(http_res, ["500", "501", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.FailureData, http_res)
            raise errors.Failure(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)

        raise errors.SDKError("Unexpected response received", http_res)
