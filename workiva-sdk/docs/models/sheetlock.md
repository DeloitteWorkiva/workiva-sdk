# SheetLock

The type of lock applied to this sheet, if any. Note this property is not tied to revision and will always reflect the sheet's current lock state. When this field is not present on the sheet object, the sheet is not locked.


## Values

| Name         | Value        |
| ------------ | ------------ |
| `LOCK`       | lock         |
| `INPUT_MODE` | inputMode    |