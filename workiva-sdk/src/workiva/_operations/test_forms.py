"""Platform API — test_forms namespace.

Auto-generated by codegen. Do not edit manually.
"""

from __future__ import annotations

from typing import Any, Optional

import httpx

from workiva._constants import _API
from workiva._operations._base import BaseNamespace


class TestForms(BaseNamespace):
    """TestForms operations."""

    _api: _API = _API.PLATFORM

    def get_test_forms(
        self,
        *,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of test forms

        Retrieves a list of [test forms](ref:testforms#testform).
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms",
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_test_forms_async(
        self,
        *,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of test forms (async)

        Retrieves a list of [test forms](ref:testforms#testform).
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms",
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def get_test_form_by_id(
        self,
        *,
        test_form_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single test form

        Retrieves a [test form](ref:testforms#testform) given its ID.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}",
            path_params={
                "testFormId": test_form_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_test_form_by_id_async(
        self,
        *,
        test_form_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single test form (async)

        Retrieves a [test form](ref:testforms#testform) given its ID.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}",
            path_params={
                "testFormId": test_form_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def test_form_export(
        self,
        *,
        test_form_id: str,
        body: TestFormExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test form export

        Asynchronously exports a [test form](ref:testforms#testform).

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/export",
            path_params={
                "testFormId": test_form_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def test_form_export_async(
        self,
        *,
        test_form_id: str,
        body: TestFormExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test form export (async)

        Asynchronously exports a [test form](ref:testforms#testform).

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/export",
            path_params={
                "testFormId": test_form_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_test_phases(
        self,
        *,
        test_form_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of test phases

        Returns a list of [test phases](ref:testforms#testphase).
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases",
            path_params={
                "testFormId": test_form_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_test_phases_async(
        self,
        *,
        test_form_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of test phases (async)

        Returns a list of [test phases](ref:testforms#testphase).
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases",
            path_params={
                "testFormId": test_form_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def get_test_phase_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrive a single test phase

        Retrieves a [test phase](ref:testforms#testphase) given its ID.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_test_phase_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrive a single test phase (async)

        Retrieves a [test phase](ref:testforms#testphase) given its ID.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def test_phase_attachment_upload(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        body: GraphAttachmentUpload,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test phase attachment upload

        Starts the process to upload and attach a file to a [test
        phase](ref:testforms#testphase) using a [graph attachment
        upload](ref:testforms#graphattachmentupload) object. The response body
        will include an `uploadUrl`. To upload the file contents, perform a PUT
        on the `uploadUrl` with the same authentication credentials and flow as
        the attachmentUpload request. For more details, see [Authentication
        documentation](ref:authentication).

        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachmentUpload",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def test_phase_attachment_upload_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        body: GraphAttachmentUpload,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test phase attachment upload (async)

        Starts the process to upload and attach a file to a [test
        phase](ref:testforms#testphase) using a [graph attachment
        upload](ref:testforms#graphattachmentupload) object. The response body
        will include an `uploadUrl`. To upload the file contents, perform a PUT
        on the `uploadUrl` with the same authentication credentials and flow as
        the attachmentUpload request. For more details, see [Authentication
        documentation](ref:authentication).

        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachmentUpload",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_test_phase_attachments(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of test phase attachments

        Returns a list of [attachments](ref:testforms#graphattachment) for a
        [test phase](ref:testforms#testphase).
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            timeout=timeout,
        )

    async def get_test_phase_attachments_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of test phase attachments (async)

        Returns a list of [attachments](ref:testforms#graphattachment) for a
        [test phase](ref:testforms#testphase).
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            timeout=timeout,
        )

    def get_test_phase_attachment_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single test phase attachment

        Retrieve a single [attachment](ref:testforms#graphattachment) by its ID.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments/{attachmentId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    async def get_test_phase_attachment_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single test phase attachment (async)

        Retrieve a single [attachment](ref:testforms#graphattachment) by its ID.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments/{attachmentId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    def test_phase_attachment_download_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test phase attachment download

        Asynchronously downloads an attachment from a [test
        phase](ref:testforms#testphase).

        Responses include a `Location` header, which indicates where to poll for
        download results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the download is ready,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the file, perform a GET on the `resourceURL`
        with the same authentication credentials and flow as the download
        request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments/{attachmentId}/download",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    async def test_phase_attachment_download_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test phase attachment download (async)

        Asynchronously downloads an attachment from a [test
        phase](ref:testforms#testphase).

        Responses include a `Location` header, which indicates where to poll for
        download results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the download is ready,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the file, perform a GET on the `resourceURL`
        with the same authentication credentials and flow as the download
        request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments/{attachmentId}/download",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    def test_phase_attachment_export_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        attachment_id: str,
        body: GraphAttachmentExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test phase attachment export

        Asynchronously exports an attachment for a [test
        phase](ref:testforms#testphase) to .PDF.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments/{attachmentId}/export",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "attachmentId": attachment_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def test_phase_attachment_export_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        attachment_id: str,
        body: GraphAttachmentExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a test phase attachment export (async)

        Asynchronously exports an attachment for a [test
        phase](ref:testforms#testphase) to .PDF.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/attachments/{attachmentId}/export",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "attachmentId": attachment_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def create_matrix(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        body: Matrix,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Create a new matrix

        Create a new empty [matrix](ref:testforms#matrix). The `id` field for
        the matrix and its columns should be left blank; this will be populated
        by the endpoint.
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def create_matrix_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        body: Matrix,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Create a new matrix (async)

        Create a new empty [matrix](ref:testforms#matrix). The `id` field for
        the matrix and its columns should be left blank; this will be populated
        by the endpoint.
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_matrices(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of matrices

        Returns a list of [matrices](ref:testforms#matrix).
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_matrices_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of matrices (async)

        Returns a list of [matrices](ref:testforms#matrix).
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def get_matrix_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single matrix

        Retrieves a [matrix](ref:testforms#matrix) given its ID.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_matrix_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single matrix (async)

        Retrieves a [matrix](ref:testforms#matrix) given its ID.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def matrix_attachment_upload(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: GraphAttachmentUpload,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a matrix attachment upload

        Starts the process to upload and attach a file to a
        [matrix](ref:testforms#matrix) using a [graph attachment
        upload](ref:testforms#graphattachmentupload) object. The response body
        will include an `uploadUrl`. To upload the file contents, perform a PUT
        on the `uploadUrl` with the same authentication credentials and flow as
        the attachmentUpload request. For more details, see [Authentication
        documentation](ref:overview#authentication).

        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachmentUpload",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def matrix_attachment_upload_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: GraphAttachmentUpload,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a matrix attachment upload (async)

        Starts the process to upload and attach a file to a
        [matrix](ref:testforms#matrix) using a [graph attachment
        upload](ref:testforms#graphattachmentupload) object. The response body
        will include an `uploadUrl`. To upload the file contents, perform a PUT
        on the `uploadUrl` with the same authentication credentials and flow as
        the attachmentUpload request. For more details, see [Authentication
        documentation](ref:overview#authentication).

        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachmentUpload",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_matrix_attachments(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of matrix attachments

        Returns a list of [attachments](ref:testforms#graphattachment) for a
        matrix.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            timeout=timeout,
        )

    async def get_matrix_attachments_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of matrix attachments (async)

        Returns a list of [attachments](ref:testforms#graphattachment) for a
        matrix.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            timeout=timeout,
        )

    def get_matrix_attachment_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single matrix attachment

        Retrieve a single [attachment](ref:testforms#graphattachment) by its ID.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments/{attachmentId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    async def get_matrix_attachment_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single matrix attachment (async)

        Retrieve a single [attachment](ref:testforms#graphattachment) by its ID.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments/{attachmentId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    def matrix_attachment_download_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a matrix attachment download

        Asynchronously downloads an attachment from a
        [matrix](ref:testforms#matrix).

        Responses include a `Location` header, which indicates where to poll for
        download results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the download is ready,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the file, perform a GET on the `resourceURL`
        with the same authentication credentials and flow as the download
        request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments/{attachmentId}/download",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    async def matrix_attachment_download_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a matrix attachment download (async)

        Asynchronously downloads an attachment from a
        [matrix](ref:testforms#matrix).

        Responses include a `Location` header, which indicates where to poll for
        download results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the download is ready,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the file, perform a GET on the `resourceURL`
        with the same authentication credentials and flow as the download
        request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments/{attachmentId}/download",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    def matrix_attachment_export_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        attachment_id: str,
        body: GraphAttachmentExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate an export of a matrix attachment

        Asynchronously exports an attachment for a
        [matrix](ref:testforms#matrix) to .PDF.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments/{attachmentId}/export",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "attachmentId": attachment_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def matrix_attachment_export_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        attachment_id: str,
        body: GraphAttachmentExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate an export of a matrix attachment (async)

        Asynchronously exports an attachment for a
        [matrix](ref:testforms#matrix) to .PDF.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/attachments/{attachmentId}/export",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "attachmentId": attachment_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def create_sample(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: MatrixSample,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Create a new sample

        Create a new [sample](ref:testforms#matrixsample) in a
        [matrix](ref:testforms#matrix). The new sample will be appended to the
        end of the matrix.
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def create_sample_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: MatrixSample,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Create a new sample (async)

        Create a new [sample](ref:testforms#matrixsample) in a
        [matrix](ref:testforms#matrix). The new sample will be appended to the
        end of the matrix.
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_samples(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of samples

        Returns a list of [samples](ref:testforms#matrixsample).
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_samples_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of samples (async)

        Returns a list of [samples](ref:testforms#matrixsample).
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def sample_insertion(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: MatrixSamples,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Insert samples

        Inserts multiple [samples](ref:testforms#matrixsamples) into a
        [matrix](ref:testforms#matrix), and appends new samples to the end of
        the matrix. You can leave columns empty for later use. For new samples,
        provide no IDs; the endpoint generates them.
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/insertion",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def sample_insertion_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: MatrixSamples,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Insert samples (async)

        Inserts multiple [samples](ref:testforms#matrixsamples) into a
        [matrix](ref:testforms#matrix), and appends new samples to the end of
        the matrix. You can leave columns empty for later use. For new samples,
        provide no IDs; the endpoint generates them.
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/insertion",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def sample_update(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: MatrixSamples,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Update samples

        Updates multiple [samples](ref:testforms#matrixsamples), with the
        requestBody of each specifying columns to update by their IDs. Columns
        not included in the request remain as-is.
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/update",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def sample_update_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        body: MatrixSamples,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Update samples (async)

        Updates multiple [samples](ref:testforms#matrixsamples), with the
        requestBody of each specifying columns to update by their IDs. Columns
        not included in the request remain as-is.
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/update",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_sample_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single sample

        Retrieves a [sample](ref:testforms#matrixsample) given its ID.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    async def get_sample_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        expand: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single sample (async)

        Retrieves a [sample](ref:testforms#matrixsample) given its ID.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            query_params={
                "$expand": expand,
            },
            timeout=timeout,
        )

    def partially_update_sample_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        body: JSONPatchDocument,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially update a single sample

        Partially updates the properties of a
        [sample](ref:testforms#matrixsample). Note: Cell values must always be
        strings, even if they represent a number.
        ### Options
        |Path|PATCH Operations Supported|
        |---|---|
        |`/dataValues/<index>/value`|`replace`|
        |`/resultValues/<index>/value`|`replace`|
        """
        return self._client.request(
            "PATCH",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def partially_update_sample_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        body: JSONPatchDocument,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Partially update a single sample (async)

        Partially updates the properties of a
        [sample](ref:testforms#matrixsample). Note: Cell values must always be
        strings, even if they represent a number.
        ### Options
        |Path|PATCH Operations Supported|
        |---|---|
        |`/dataValues/<index>/value`|`replace`|
        |`/resultValues/<index>/value`|`replace`|
        """
        return await self._client.request_async(
            "PATCH",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def sample_attachment_upload(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        body: GraphAttachmentUpload,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate an upload of a sample attachment

        Starts the process to upload and attach a file to a
        [sample](ref:testforms#matrixsample) using a [graph attachment
        upload](ref:testforms#graphattachmentupload) object. The response body
        will include an `uploadUrl`. To upload the file contents, perform a PUT
        on the `uploadUrl` with the same authentication credentials and flow as
        the attachmentUpload request. For more details, see [Authentication
        documentation](ref:authentication).

        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachmentUpload",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def sample_attachment_upload_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        body: GraphAttachmentUpload,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate an upload of a sample attachment (async)

        Starts the process to upload and attach a file to a
        [sample](ref:testforms#matrixsample) using a [graph attachment
        upload](ref:testforms#graphattachmentupload) object. The response body
        will include an `uploadUrl`. To upload the file contents, perform a PUT
        on the `uploadUrl` with the same authentication credentials and flow as
        the attachmentUpload request. For more details, see [Authentication
        documentation](ref:authentication).

        The response also includes a `Location` header, which indicates where to
        poll for operation results. For more details on long-running job
        polling, see [Operations endpoint](ref:getoperationbyid).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachmentUpload",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            json_body=body,
            timeout=timeout,
        )

    def get_sample_attachments(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of sample attachments

        Returns a list of [attachments](ref:testforms#graphattachment) for a
        [sample](ref:testforms#matrixsample).
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            timeout=timeout,
        )

    async def get_sample_attachments_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a list of sample attachments (async)

        Returns a list of [attachments](ref:testforms#graphattachment) for a
        [sample](ref:testforms#matrixsample).
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
            },
            timeout=timeout,
        )

    def get_sample_attachment_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single sample attachment

        Retrieve a single [attachment](ref:testforms#graphattachment) by its ID.
        """
        return self._client.request(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments/{attachmentId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    async def get_sample_attachment_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Retrieve a single sample attachment (async)

        Retrieve a single [attachment](ref:testforms#graphattachment) by its ID.
        """
        return await self._client.request_async(
            "GET",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments/{attachmentId}",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    def sample_attachment_download_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a download of a sample attachment

        Asynchronously downloads an attachment from a
        [sample](ref:testforms#matrixsample).

        Responses include a `Location` header, which indicates where to poll for
        download results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the download is ready,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the file, perform a GET on the `resourceURL`
        with the same authentication credentials and flow as the download
        request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments/{attachmentId}/download",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    async def sample_attachment_download_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        attachment_id: str,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate a download of a sample attachment (async)

        Asynchronously downloads an attachment from a
        [sample](ref:testforms#matrixsample).

        Responses include a `Location` header, which indicates where to poll for
        download results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the download is ready,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the file, perform a GET on the `resourceURL`
        with the same authentication credentials and flow as the download
        request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments/{attachmentId}/download",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
                "attachmentId": attachment_id,
            },
            timeout=timeout,
        )

    def sample_attachment_export_by_id(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        attachment_id: str,
        body: GraphAttachmentExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate an export of a sample attachment

        Asynchronously exports an attachment for a
        [sample](ref:testforms#matrixsample) to .PDF.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return self._client.request(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments/{attachmentId}/export",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
                "attachmentId": attachment_id,
            },
            json_body=body,
            timeout=timeout,
        )

    async def sample_attachment_export_by_id_async(
        self,
        *,
        test_form_id: str,
        test_phase_id: str,
        matrix_id: str,
        sample_id: str,
        attachment_id: str,
        body: GraphAttachmentExport,
        timeout: Optional[float] = None,
    ) -> httpx.Response:
        """Initiate an export of a sample attachment (async)

        Asynchronously exports an attachment for a
        [sample](ref:testforms#matrixsample) to .PDF.

        Responses include a `Location` header, which indicates where to poll for
        export results. For more details on long-running job polling, see
        [Operations endpoint](ref:getoperationbyid). When the export completes,
        its status will be `completed`, and the response body includes a
        `resourceURL`. To download the exported file, perform a GET on the
        `resourceURL` with the same authentication credentials and flow as the
        export request. For more details, see [Authentication
        documentation](ref:authentication).
        """
        return await self._client.request_async(
            "POST",
            self._api,
            "/testForms/{testFormId}/testPhases/{testPhaseId}/matrices/{matrixId}/samples/{sampleId}/attachments/{attachmentId}/export",
            path_params={
                "testFormId": test_form_id,
                "testPhaseId": test_phase_id,
                "matrixId": matrix_id,
                "sampleId": sample_id,
                "attachmentId": attachment_id,
            },
            json_body=body,
            timeout=timeout,
        )
