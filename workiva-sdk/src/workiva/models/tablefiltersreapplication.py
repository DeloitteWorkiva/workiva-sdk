"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from workiva.types import BaseModel, UNSET_SENTINEL


class TableFiltersReapplicationTypedDict(TypedDict):
    r"""A table filter reapply operation, which can reapply filters to a table and optionally force hide footnotes."""

    force_hide_footnotes: NotRequired[bool]
    r"""Whether the filter should be reapplied if doing so would cause footnotes to be hidden."""


class TableFiltersReapplication(BaseModel):
    r"""A table filter reapply operation, which can reapply filters to a table and optionally force hide footnotes."""

    force_hide_footnotes: Annotated[
        Optional[bool], pydantic.Field(alias="forceHideFootnotes")
    ] = False
    r"""Whether the filter should be reapplied if doing so would cause footnotes to be hidden."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["forceHideFootnotes"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
