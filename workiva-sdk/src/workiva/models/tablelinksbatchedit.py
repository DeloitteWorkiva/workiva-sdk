"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .lockedcelleditmode import LockedCellEditMode
from .tablelinksedit import TableLinksEdit, TableLinksEditTypedDict
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from workiva.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL


class TableLinksBatchEditTypedDict(TypedDict):
    r"""A collection of link edits to perform on table content."""

    data: List[TableLinksEditTypedDict]
    r"""The edits to apply to the table."""
    locked_cell_edit_mode: NotRequired[LockedCellEditMode]
    r"""Whether or not an edit applied to locked cells is prevented. Validation will prevent an edit with a locked
    cell when set to `strict`. When set to `trim` the edit will succeed with the locked cells trimmed out.

    """
    revision: NotRequired[Nullable[str]]
    r"""Identifies the version of the table to apply the edits to. Defaults to the latest revision if not specified.

    """


class TableLinksBatchEdit(BaseModel):
    r"""A collection of link edits to perform on table content."""

    data: List[TableLinksEdit]
    r"""The edits to apply to the table."""

    locked_cell_edit_mode: Annotated[
        Optional[LockedCellEditMode], pydantic.Field(alias="lockedCellEditMode")
    ] = LockedCellEditMode.STRICT
    r"""Whether or not an edit applied to locked cells is prevented. Validation will prevent an edit with a locked
    cell when set to `strict`. When set to `trim` the edit will succeed with the locked cells trimmed out.

    """

    revision: OptionalNullable[str] = UNSET
    r"""Identifies the version of the table to apply the edits to. Defaults to the latest revision if not specified.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["lockedCellEditMode", "revision"])
        nullable_fields = set(["revision"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
