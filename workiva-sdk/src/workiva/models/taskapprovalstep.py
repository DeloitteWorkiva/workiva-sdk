"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .taskapprovalresponse import TaskApprovalResponse, TaskApprovalResponseTypedDict
from .taskuser import TaskUser, TaskUserTypedDict
from datetime import datetime
from enum import Enum
import pydantic
from pydantic import field_serializer, model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from workiva import models, utils
from workiva.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL


class CompletionMode(str, Enum, metaclass=utils.OpenEnumMeta):
    ONE = "ONE"
    ALL = "ALL"


class TaskApprovalStepTypedDict(TypedDict):
    r"""An approval step in the approval process for a task. Each step defines how completion is determined and who participates."""

    completion_mode: CompletionMode
    participants: List[TaskUserTypedDict]
    r"""The participants in this approval step."""
    due_date: NotRequired[Nullable[datetime]]
    r"""An ISO 8601 datetime indicating a deadline for this approval step, or null if not set."""
    responses: NotRequired[List[TaskApprovalResponseTypedDict]]
    r"""All APPROVE, REJECT, and SKIP actions for the current approval process are tracked in the `response` for each step. This surfaces any comments left, and allows for tracking the progress of the approval process. When a REJECT action is left, responses will remain on the task until the task is re-submitted for approval.

    """


class TaskApprovalStep(BaseModel):
    r"""An approval step in the approval process for a task. Each step defines how completion is determined and who participates."""

    completion_mode: Annotated[CompletionMode, pydantic.Field(alias="completionMode")]

    participants: List[TaskUser]
    r"""The participants in this approval step."""

    due_date: Annotated[OptionalNullable[datetime], pydantic.Field(alias="dueDate")] = (
        UNSET
    )
    r"""An ISO 8601 datetime indicating a deadline for this approval step, or null if not set."""

    responses: Optional[List[TaskApprovalResponse]] = None
    r"""All APPROVE, REJECT, and SKIP actions for the current approval process are tracked in the `response` for each step. This surfaces any comments left, and allows for tracking the progress of the approval process. When a REJECT action is left, responses will remain on the task until the task is re-submitted for approval.

    """

    @field_serializer("completion_mode")
    def serialize_completion_mode(self, value):
        if isinstance(value, str):
            try:
                return models.CompletionMode(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["dueDate", "responses"])
        nullable_fields = set(["dueDate"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class TaskApprovalStepInputTypedDict(TypedDict):
    r"""An approval step in the approval process for a task. Each step defines how completion is determined and who participates."""

    completion_mode: CompletionMode
    participants: List[TaskUserTypedDict]
    r"""The participants in this approval step."""
    due_date: NotRequired[Nullable[datetime]]
    r"""An ISO 8601 datetime indicating a deadline for this approval step, or null if not set."""


class TaskApprovalStepInput(BaseModel):
    r"""An approval step in the approval process for a task. Each step defines how completion is determined and who participates."""

    completion_mode: Annotated[CompletionMode, pydantic.Field(alias="completionMode")]

    participants: List[TaskUser]
    r"""The participants in this approval step."""

    due_date: Annotated[OptionalNullable[datetime], pydantic.Field(alias="dueDate")] = (
        UNSET
    )
    r"""An ISO 8601 datetime indicating a deadline for this approval step, or null if not set."""

    @field_serializer("completion_mode")
    def serialize_completion_mode(self, value):
        if isinstance(value, str):
            try:
                return models.CompletionMode(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["dueDate"])
        nullable_fields = set(["dueDate"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    TaskApprovalStep.model_rebuild()
except NameError:
    pass
try:
    TaskApprovalStepInput.model_rebuild()
except NameError:
    pass
