"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .lineterminator import LineTerminator, LineTerminatorTypedDict
from .margins import Margins, MarginsTypedDict
from .stroke import Stroke, StrokeTypedDict
from .wrapstyletype import WrapStyleType
import pydantic
from pydantic import field_serializer, model_serializer
from typing_extensions import Annotated, TypedDict
from workiva import models
from workiva.types import BaseModel, Nullable, UNSET_SENTINEL


class LineTypedDict(TypedDict):
    r"""A drawing element for lines, arrows, connectors, etc. All of these elements have two points: a head and tail."""

    head: LineTerminatorTypedDict
    r"""Defines the shape, size, and location of a line''s head and tail
    (the starting point and stop point of a line segment or connector).

    """
    id: str
    r"""Identifier of the drawing element"""
    length: float
    r"""The length of the line in points."""
    margins: Nullable[MarginsTypedDict]
    r"""The margins of a rectangle for properties of objects such as a shape, text box, or page.

    """
    stroke: Nullable[StrokeTypedDict]
    r"""Line stroke setting for things like shape edges. Use a null to indicate no stroke line."""
    tail: LineTerminatorTypedDict
    r"""Defines the shape, size, and location of a line''s head and tail
    (the starting point and stop point of a line segment or connector).

    """
    wrap_style: Nullable[WrapStyleType]
    r"""The relative position type"""


class Line(BaseModel):
    r"""A drawing element for lines, arrows, connectors, etc. All of these elements have two points: a head and tail."""

    head: LineTerminator
    r"""Defines the shape, size, and location of a line''s head and tail
    (the starting point and stop point of a line segment or connector).

    """

    id: str
    r"""Identifier of the drawing element"""

    length: float
    r"""The length of the line in points."""

    margins: Nullable[Margins]
    r"""The margins of a rectangle for properties of objects such as a shape, text box, or page.

    """

    stroke: Nullable[Stroke]
    r"""Line stroke setting for things like shape edges. Use a null to indicate no stroke line."""

    tail: LineTerminator
    r"""Defines the shape, size, and location of a line''s head and tail
    (the starting point and stop point of a line segment or connector).

    """

    wrap_style: Annotated[Nullable[WrapStyleType], pydantic.Field(alias="wrapStyle")]
    r"""The relative position type"""

    @field_serializer("wrap_style")
    def serialize_wrap_style(self, value):
        if isinstance(value, str):
            try:
                return models.WrapStyleType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                m[k] = val

        return m
