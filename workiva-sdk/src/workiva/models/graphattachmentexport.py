"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from workiva.types import BaseModel, UNSET_SENTINEL


class GraphAttachmentExportFormat(str, Enum):
    r"""The format to export the attachment to — currently, .PDF only"""

    PDF = "pdf"


class GraphAttachmentExportTypedDict(TypedDict):
    r"""Details about the attachment export including its format"""

    format_: NotRequired[GraphAttachmentExportFormat]
    r"""The format to export the attachment to — currently, .PDF only"""


class GraphAttachmentExport(BaseModel):
    r"""Details about the attachment export including its format"""

    format_: Annotated[
        Optional[GraphAttachmentExportFormat], pydantic.Field(alias="format")
    ] = None
    r"""The format to export the attachment to — currently, .PDF only"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["format"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    GraphAttachmentExport.model_rebuild()
except NameError:
    pass
